apiVersion: v1
kind: Service

metadata:
  name: barcode-service

spec:
  type: NodePort

  selector:
    app: barcode

  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080